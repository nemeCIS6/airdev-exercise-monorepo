# Convex (written by `npx convex dev`)
NEXT_PUBLIC_CONVEX_URL=
CONVEX_DEPLOYMENT=

# Convex Auth (written by `npx @convex-dev/auth`)
JWT_PRIVATE_KEY=
JWKS=
SITE_URL=http://localhost:3000

# Gemini OCR — set when implementing convex/ai.ts (deferred §8 feature)
GEMINI_API_KEY=
